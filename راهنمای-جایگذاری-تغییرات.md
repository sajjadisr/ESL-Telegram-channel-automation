# راهنمای جایگذاری این نسخه‌ی جدید

## چی تغییر کرده (خلاصه)

| مشکل قبلی | راه‌حل این نسخه |
|---|---|
| هر روز یک فرمت (توضیح+مثال) | `data/format_schedule.json` — فرمت روز از روی روز هفته تعیین می‌شه، ۷ فرمت متفاوت |
| بدون الزام قلاب | هر پرامپت صریحاً قلاب می‌خواد و ریویو هم قلاب رو چک می‌کنه |
| بدون سیستم 🟢🟡🔴 | داخل پرامپت اومده، برای فرمت‌هایی که لازمشون دارن |
| بدون کوییز/نظرسنجی واقعی | `telegram_bot.send_poll()` با `sendPoll` واقعی تلگرام (quiz + regular) |
| Markdown به‌جای HTML | `parse_mode: "HTML"` — اسپویلر و بولد الان واقعاً کار می‌کنن |
| بدون خط‌لوله‌ی تصویر | فرمت `illustrated_pun` پرامپت تصویر می‌سازه و به شما (ادمین) پیام خصوصی می‌ده — به‌جای پست خودکار بدون عکس |
| بدون کنترل رجیستر فارسی | چک‌لیست محاوره‌ای/نیم‌فاصله/calque داخل پرامپت |
| Curriculum خطی، بدون بازآوری | فرمت `progress_recap` هر ۱۴ پست خودکار فعال می‌شه؛ کوییز/نظرسنجی از پست‌های اخیر می‌سازه، نه موضوع تصادفی |
| «هر سطحی» | همه‌جا مبتدی‌محور (A1–A2)؛ `data/topics.json` فقط A1/A2 داره |

## جایگذاری روی ریپوی فعلی‌تون

```bash
cd ~/Desktop/frontend2/english-channel-ai
source venv/bin/activate   # اگه venv ساختید

# فایل‌های پایتون و JSON رو جایگزین کنید (این‌ها رو از پوشه‌ی این پکیج کپی کنید):
# config.py, database.py, memory.py, ai.py, prompts.py, telegram_bot.py, main.py,
# feedback_add.py, weekly_strategy.py, requirements.txt
# data/memory.json, data/strategy.json, data/feedback.json, data/topics.json
# data/format_schedule.json  ← جدید
# data/story.json            ← جدید
# .github/workflows/daily_post.yml, .github/workflows/weekly_strategy.yml

# اگه data/posts.db از قبل دارید، لازم نیست پاکش کنید — database.py خودش
# ستون جدید «format» رو موقع اولین اجرا اضافه می‌کنه (migration خودکار).

git add .
git commit -m "Rebuild content engine: format rotation, real polls, beginner-only scope"
git push
```

## یه Secret اختیاری جدید (برای فرمت illustrated_pun)

چون این پروژه به هیچ تولیدکننده‌ی تصویری وصل نیست، وقتی نوبت فرمت «شوخی تصویری» می‌شه، ربات به‌جای پست خودکار (که بدون عکس ناقص می‌بود)، کپشن آماده + پرامپت تصویر رو **به خود شما** پیام می‌ده تا عکس رو دستی بسازید و با هم پست کنید.

اگه می‌خواید این پیام‌ها رو توی تلگرام دریافت کنید:
1. به [@userinfobot](https://t.me/userinfobot) پیام بدید تا `chat id` عددی خودتون رو بگیرید.
2. بات کانال (همون که قبلاً ساختید) رو با یه `/start` باز کنید تا اجازه‌ی پیام‌دادن بهتون رو داشته باشه.
3. توی **Settings → Secrets → Actions** یه Secret جدید بسازید: `TELEGRAM_ADMIN_CHAT_ID` با مقدار همون chat id.

اگه این Secret رو نسازید، مشکلی نیست — پرامپت‌ها به‌جاش توی لاگ تب **Actions** چاپ می‌شن و از همون‌جا کپی‌شون می‌کنید.

## تست محلی قبل از فعال‌سازی

```bash
export TELEGRAM_BOT_TOKEN="..."
export TELEGRAM_CHANNEL_ID="@your_english_channel"
export GEMINI_API_KEY="..."
export TELEGRAM_ADMIN_CHAT_ID="..."   # اختیاری

python main.py
```

چون فرمت امروز از روی روز هفته‌ی واقعی سیستم شما تعیین می‌شه، برای تست فرمت‌های دیگه می‌تونید موقتاً `data/format_schedule.json` رو ویرایش کنید (یا مقدار `resolve_today_format()` رو توی `main.py` دستی برای یه تست عوض کنید و بعد برگردونید).

## نکته درباره‌ی مبتدی‌محور بودن

از این به بعد فقط موضوعات A1/A2 به `data/topics.json` اضافه کنید — هر موضوع B1 به بالا (مثل Present perfect، Passive voice، Business idioms) عمداً از این نسخه حذف شده چون با تصمیم «فقط مبتدی» جور در نمی‌اومد.
